var paginationController = function(objEvent, currentPage) {
  var currentPage = currentPage;
  var nextPage = currentPage + 1;
  var previusPage = currentPage - 1;
  var numberOfShowChar = 10; // limitCharacters

  var totalCharacters = 1485; // $.ajax data.data.total from characters
  var mumberOfPages = 1485 / numberOfShowChar;
  mumberOfPages = Math.floor(mumberOfPages) + 1




  // UIController

  // End UIController

}
